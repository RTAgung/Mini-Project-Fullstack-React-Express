export interface TryoutSectionModel {
    id: string;
    code: string;
    description: string;
    title: string;
    order: number;
    data: string;
    tag: string;
    active: boolean;
    createAt: string;
    updateAt: string;
}
